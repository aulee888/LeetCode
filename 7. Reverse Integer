class Solution:
    def reverse(self, x: int) -> int:
        output = ''
        list = []
        
        for i in range(len(str(x))):
            list.append(str(x)[i])
        for j in range(len(list)):
            output = output + list.pop()
            
        return int(output)
